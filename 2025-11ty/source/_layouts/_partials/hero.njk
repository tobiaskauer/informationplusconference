{% set header_image = header.image | default('t2-blue-magenta') %}
{% set hero_video_src = header.media_url or hero_videos[header.vimeo_id] or hero_videos['1106005730'] %}
<section class="hero">
  <div class="header-video">
    <!-- poster image as background -->
      <picture class="video-poster">
        <source srcset="{{ ('/images/headers/' ~ header_image ~ '.avif') | url }}" type="image/avif">
        <source srcset="{{ ('/images/headers/' ~ header_image ~ '.webp') | url }}" type="image/webp">
        <img src="{{ ('/images/headers/' ~ header_image ~ '.png') | url }}"
            alt="Poster"
            style="width: 100%; height: 100%; object-fit: cover;">
      </picture>
      <!-- MediaCMS-hosted hero background video -->
      <video
        class="hero-background-video"
        autoplay
        loop
        muted
        playsinline
        preload="metadata"
        poster="{{ ('/images/headers/' ~ header_image ~ '.png') | url }}"
        aria-hidden="true">
        <source src="{{ hero_video_src }}" type="video/mp4">
      </video>
  </div>
  <div class="hero-body">
    <div class="container">
      <div class="columns">
        <div class="column is-one-half" style="align-content: end;">
          <div class="logo">
          {% include "../../../source/images/logos/infoplus_logo_anim.svg" %}
          </div>
        </div>
      </div>
      <div class="columns">
        <div class="column is-one-half" style="align-content: end;">
          <h4 class="eyebrow">{{ global.hero_intro.title }}</h4>
          {{ global.hero_intro.text | markdownify | safe }}
        </div>
        <div class="column is-one-quarter" style="align-content: end;">
          &nbsp;
        </div>
        <div class="column is-one-quarter content" style="align-content: end;">
          {{ global.call_to_action | markdownify | safe }}
        </div>
      </div>
      <div class="columns">
        <div class="column is-one-half medium-text content" style="align-content: end;">
          {{ global.event_date | markdownify | safe }}
        </div>
        <div class="column">&nbsp;</div>
      </div>
    </div>
  </div>
</section>